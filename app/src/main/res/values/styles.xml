<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/whiteV2</item>
        <item name="colorAccent">@color/whiteV1</item>
    </style>
    <style name="AppTheme.Default" parent="@android:style/Theme"></style>
    <style name="AppTheme.NoTitle" parent="@android:style/Theme.NoTitleBar"></style>
    <style name="AppTheme.FullScreen" parent="@android:style/Theme.NoTitleBar.Fullscreen"></style>

</resources>
